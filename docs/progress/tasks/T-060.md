---
id: T-060
title: Decide whether an agent may be called `agent-`
milestone: M5
status: done
owner: subagent-T-060
branch: task/T-060-decide-whether-an-agent-may
pr: 105
estimate: S
depends_on: [T-059]
paths:
  - packages/protocol/src/schemas/primitives.ts
  - packages/protocol/src/version.ts
  - server/src/db/schema/agents.ts
  - server/drizzle
  - scripts/upgrade-test.sh
  - docs/IMPLEMENTATION-PLAN.md
  - docs/protocol.md
blocked_reason: null
---

# T-060 — Decide whether an agent may be called `agent-`

## Decided: narrow it. One shape for every handle a person says out loud.

The maintainer settled this, and the argument is one nobody in this thread had made. **A name has to survive a round trip through human speech.**

The workflow is not a user typing an address. It is a user telling their harness *"check the implementation with alice's backend agent"*, and the harness querying the agent listing and resolving that phrase to `@alice/backend`. A name is therefore something a person says, an agent matches, and a machine passes along — and `backend-` or `back--end` is none of those things. It cannot be spoken unambiguously, it reads as a typo, and it makes fuzzy resolution against a listing worse for no gain.

**The identifier is where irregular shapes belong.** Agents already have `agt_<uuidv7>`, which is exact, opaque and never spoken. Anything that needs to be precise uses it. The name is the human half, and the human half should look human.

So the three grammars converge on one *shape* — runs of lowercase alphanumerics joined by single hyphens, no leading or trailing hyphen — while keeping the ceilings each has a reason for: 32 for an agent name and a slug, 39 for a username because that is GitHub's rule and the username is not ours to choose.

A user who types all three handles meets two different rules.

| Handle | `agent-` | `a--b` |
|---|---|---|
| Agent name | **accepted** | **accepted** |
| Project slug | rejected | rejected |
| Username | rejected | rejected |

**This is not a defect and there is nothing broken to fix.** Agent names have never had the boundary-versus-storage fault that slugs and usernames had: `agents_name_format` transcribes `AGENT_NAME_PATTERN` character for character, so the two sides agree exactly and nothing passes validation only to die at storage. Verified in T-059 against a live database, over 390 candidates, with zero disagreements.

It is a product question instead: what may an agent be called, and should the answer be the same shape a user already learned for their username and their projects?

## Why it needs deciding rather than leaving

`@alice/backend` is one string a person reads and types, and its two halves currently obey different grammars. `alice--bob` is not a username, but `alice/my--agent` is a valid address. Nobody has hit this, and it is the kind of thing that is obvious in hindsight the first time somebody does.

## What makes it expensive

Narrowing the agent-name grammar is a **third** break of the wire contract, after usernames (T-016) and slugs (T-025). The snapshot guard will refuse it until `PROTOCOL_VERSION` is raised again and a reason is recorded, which is the right friction but is not free. It also means a migration question for any existing agent names — of which there are none outside development today, and that will not be true after the first release.

**The window is now.** Before a tag this costs a version bump and a paragraph. After one it costs a rename path for real users.

## Acceptance criteria

- [ ] A decision, argued, not a preference. Either the three grammars converge, or agent names stay as they are and the plan says why the asymmetry is acceptable rather than merely tolerated.
- [ ] Weigh at least: one rule for a user to learn versus a third contract break; whether `-` at the end of a name is ever *wanted*; and whether the address grammar `@user/agent` should be stated once as a whole rather than as two halves that happen to compose.
- [ ] If narrowed: `PROTOCOL_VERSION` raised, the ledger entry written, `agents_name_format` moved with it, and the change verified against a live database by attempting real inserts — the method T-059 used, not by reading regexes.
- [ ] If not narrowed: D18's "left deliberately unsettled" paragraph is replaced by a decision with its reasoning, so the next reader is not invited to reopen it.
- [ ] `docs/protocol.md` agrees either way. It is currently correct for all three grammars.

## Notes

Raised by T-059, which brought the plan into line with the slug narrowing and deliberately declined to settle this in a documentation task. Read D17 and D18 in `docs/IMPLEMENTATION-PLAN.md` — D17 is struck in place rather than deleted, so the reasoning that produced the original one-grammar decision is still there, and it is the argument to engage with.

Read T-016 and T-025 for how the other two grammars were narrowed and what the acceptance path costs in practice.

## Log

- 2026-09-09 — Claimed by subagent-T-060.
- 2026-09-09 — Widened declared paths, deliberately and recorded here rather than silently: packages/protocol/src/version.ts (PROTOCOL_VERSION 3 -> 4 is required by the snapshot acceptance path) and server/drizzle (narrowing agents_name_format needs a migration; the original list named only the schema module). No other task is in_progress, so nothing collides. docs/cli.md also quotes the grammar and is NOT declared here — reported to the orchestrator instead of reached for.
- 2026-09-09 — Declared scripts/upgrade-test.sh, a second deliberate widening. 0004 is the first real migration to carry a '-- contract-step:' marker, and selftest_case ran its synthetic migration on top of PREVIOUS_CUT, so the applied range included the tree's newest real migration. applied_migrations_carry_marker is coarse by design — one marker anywhere in the range excuses every removal in it — so my marker was excusing the unmarked drop three selftest cases exist to catch, and 'upgrade-test.sh selftest' (which CI runs) reported a check that could no longer fail as passing. Confirmed by removing the marker and rerunning. Fixed by cutting the selftest at the whole real journal, so the upgrade under test is only the synthetic migration, which is what each case describes. Latent harness defect, not caused by this change but exposed by it; flagged for review in the PR.
- 2026-09-09 — Status in_progress -> in_review (PR #105).
- 2026-09-09 — Opened PR #105. Narrowed AGENT_NAME_PATTERN and agents_name_format to the single-hyphen shape, PROTOCOL_VERSION 3 -> 4 with ten ledger entries, migration 0004 with no backfill (502 names, zero violations, measured). Verified by real inserts before and after the migration: 2 disagreements before, 0 after. Removed rather than inverted the primitives test asserting the slug and agent-name patterns differ — an equality would restore the coupling D18 deleted; the containment invariant carries what mattered. Also fixed scripts/upgrade-test.sh, whose selftest was silently disarmed by the first real contract-step marker. docs/cli.md still quotes the old grammar and protocolVersion 3, and is outside this task's paths.
- 2026-09-09 — Status in_review -> done (PR #105).
