---
id: T-313
title: Inbox, conversation and acknowledgement commands
milestone: M3
status: done
owner: subagent-T-313
branch: task/T-313-inbox-conversation-and-acknowledgement-commands
pr: 67
estimate: M
depends_on: [T-205, T-305]
paths:
  - packages/cli/src/commands/inbox.ts
  - packages/cli/src/commands/conversation.ts
  - packages/cli/src/commands/ack.ts
blocked_reason: null
---

# T-313 — Inbox, conversation and acknowledgement commands

Reading messages without holding a connection open, for agents that prefer polling and for humans debugging a delivery.

## Acceptance criteria

- [ ] `agentchat inbox` shows pending messages by default and recent history with `--all`.
- [ ] `agentchat conversation <id>` prints a thread in chronological order with senders.
- [ ] `agentchat ack` acknowledges one or more messages, for use alongside `listen --no-ack`.
- [ ] All three support `--json` with a documented shape.
- [ ] Tests cover each command against a mock server.

## Notes

Plan §6.2.

## Log

- 2026-09-09 — Claimed by subagent-T-313.
- 2026-09-09 — Network failure killed the agent; its three commits survived on the branch, including the protocol shapes moved out of the server routes.
- 2026-09-09 — Recovered the killed agent's three commits and rebased them onto main; they needed no changes. Two gaps it had not reached: the three commands were absent from server-resolution.test.ts's registry mirror, which fails by design when a command is added without classifying how much of a server it needs, and all three require an address.
- 2026-09-09 — The message shape claim was stale, and the answer changed. The module said listen would import messageJson; T-312's branch shows it passes the delivery envelope through verbatim instead, so a field a newer server adds reaches a consumer without a CLI release. That is right for a stream, so the promise is restated as the checkable one — every field listen streams is in the polled item under the same name, MessageEnvelope's nine plus recipient — and a test asserts the field lists in both directions.
- 2026-09-09 — The list is copied from server/src/routing/delivery.ts rather than imported: packages/ is MIT and server/ is AGPL, so the import that would keep the two shapes in step is the one dependency the repository forbids. An unresolved sender stays absent from the frame and null in the document — §12.4 makes omission additive-safe on a wire, while items is consumed as a table — and that difference is documented rather than smoothed over.
- 2026-09-09 — Paged to a ceiling of ten pages, a thousand messages, shared by inbox and conversation. The cursor is followed rather than a first page printed, because the part of a live thread everybody wants is the end; the ceiling bounds memory, not patience, since D10 caps one message at 1 MiB. Past it: complete false, a nextCursor, --after to resume, and the warning on stderr so --json stdout stays parseable.
- 2026-09-09 — Status in_progress -> in_review (PR #67).
- 2026-09-09 — Status in_review -> done.
- 2026-09-09 — Caught by the set-based test T-030 built: the registry mirror refuses a command nobody has classified, so all three had to state in writing how much of a server they need. The previous agent's gates passed against its old base and failed against main.
- 2026-09-09 — The polled message shape contains the streamed one rather than matching it, and that property is asserted in both directions so a field added to one and not the other fails. The list is copied rather than imported, because the import that would keep them in step crosses the licence boundary.
- 2026-09-09 — Paging follows the cursor for ten pages rather than truncating, because a thread read that shows the oldest hundred and falls silent is worse than useless. The ceiling bounds memory, not patience.
