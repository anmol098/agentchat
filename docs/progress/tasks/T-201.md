---
id: T-201
title: Protocol schemas for authentication, projects and agents
milestone: M2
status: done
owner: subagent-T-201
branch: task/T-201-protocol-schemas-for-authentication-projects
pr: null
estimate: M
depends_on: [T-005]
paths:
  - packages/protocol/src/schemas
  - packages/protocol/src/index.ts
blocked_reason: null
---

# T-201 — Protocol schemas for authentication, projects and agents

Typed request and response contracts shared by the server and the client, so a change to either side that breaks the other fails to compile.

## Acceptance criteria

- [ ] Zod schemas exist for every M1 endpoint, with inferred TypeScript types exported alongside.
- [ ] Schemas are the single definition used by both the server routes and the client methods.
- [ ] Every exported schema has a TSDoc comment.
- [ ] Round-trip parsing tests cover the representative payloads.

## Notes

Plan section 3, section 5. No hand-written duplicate interfaces anywhere.

Builds directly on T-005, which is done: read its Log and the source of `packages/protocol` first. Branded id types, the frozen error codes, and `ProtocolError` all already exist and must be composed rather than re-invented. Owns the package index so it can export what it adds.

## Log

- 2026-09-08 — Claimed by subagent-T-201.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Author flagged the bare-array list shape for a decision instead of settling it alone. Changed to an envelope at review: cheap now, a coordinated break once the client lands.
- 2026-09-08 — Two user views by design. The full one carries an email and goes only to the user it describes; invite preview answers a caller who is a member of nothing, where a full user would have been a privacy bug.
- 2026-09-08 — Found that revoked_at exists in the data model with no route to write it. Filed as T-014.
