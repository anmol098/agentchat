---
id: T-009
title: Licence boundary enforcement
milestone: M0
status: done
owner: subagent-T-009
branch: task/T-009-licence-boundary-enforcement
pr: null
estimate: M
depends_on: [T-004]
paths:
  - scripts/check-licenses.mjs
  - .github/workflows/licenses.yml
blocked_reason: null
---

# T-009 — Licence boundary enforcement

The repository is split-licensed: `packages/` is MIT and `server/` is AGPL-3.0-or-later. MIT code may be absorbed into an AGPL work, but never the reverse, so the dependency direction is a legal boundary rather than an architectural preference. A single import from `packages/` into `server/` would silently relicense the permissive half of the project. This check makes that mistake impossible to merge.

## Acceptance criteria

- [ ] A script fails when any file under `packages/` imports, requires, or re-exports anything under `server/`, including through a path alias or a dynamic import.
- [ ] The check covers the workspace dependency graph as well as source imports, so a package manifest cannot introduce the dependency either.
- [ ] Every package manifest declares the correct `license` field, and a mismatch with the path is an error.
- [ ] A dependency added under `packages/` whose licence is not permissive (MIT, ISC, BSD, or Apache-2.0) fails the check.
- [ ] Failure messages explain the licensing consequence, not just the rule, so the next contributor understands why it matters.
- [ ] The check runs in continuous integration on every pull request.

## Notes

Plan §1 and decision D16, and the root [LICENSE](../../../LICENSE) file. This is the only task whose failure mode is legal rather than technical, which is why it is enforced by a machine instead of a review checklist.

## Log

- 2026-09-08 — Claimed by subagent-T-009.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Selftest constructs every violation in a throwaway tree and CI runs it before the real check, so a green job means the checker was capable of failing. Verified three real violations against the actual tree at review.
- 2026-09-08 — Its own verification found a bug the selftest would otherwise have hidden: a regex comment stripper was deleting every tsconfig's path aliases, so the alias and project-reference checks were passing by inspecting nothing.
- 2026-09-08 — Rejects a bare 'BSD' licence string on purpose. It does not say which BSD, and the four-clause variant carries an advertising requirement MIT does not.
