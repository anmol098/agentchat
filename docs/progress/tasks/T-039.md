---
id: T-039
title: The protocol snapshot check does not know which way a shape travels
milestone: M5
status: done
owner: subagent-T-039
branch: task/T-039-the-protocol-snapshot-check-does
pr: 82
estimate: S
depends_on: [T-508]
paths:
  - scripts/protocol-snapshot.mjs
  - scripts/protocol-snapshot.json
  - tests/unit/protocol-snapshot.test.ts
blocked_reason: null
---

# T-039 — The protocol snapshot check does not know which way a shape travels

The compatibility guard treats every schema the same, but requests and responses break in opposite directions.

Adding a required field to a **request** is breaking: an old client sends a body without it and is refused. Adding a required field to a **response** is not: an old client receives a field it ignores. The guard reports both as breaking, so it demanded a major version bump for a response field that could not break anyone.

That was worked around by making the field optional, which is defensible on its own terms since a client may be talking to an older server. But the workaround and the correct answer happened to coincide, and next time they may not: the guard's advice will be to weaken a contract for a compatibility reason that does not apply.

The reverse gap is worse. Removing a field from a **response** is breaking and removing one from a **request** usually is not, so a guard that cannot tell them apart is lenient in exactly the direction that matters.

## Acceptance criteria

- [x] The snapshot records which direction each schema travels, and the comparison applies the right rule to each.
- [x] A required field added to a response is compatible; to a request, breaking.
- [x] A field removed from a response is breaking; from a request, judged deliberately and documented either way.
- [x] Existing accepted breaking changes in the ledger stay accepted; do not invalidate history to fix the rule.
- [x] Self-test cases for each of the four combinations, following the precedent that a green run means the checker was capable of failing.

## Notes

Found by T-014 while adding an identifier to a response and being told it needed a major bump.

The direction is knowable: the schemas are named by convention, request shapes and response shapes are distinguishable, and the endpoint groups are explicit. Prefer deriving it over a hand-maintained list, which would drift.

## Log

- 2026-09-08 — Found by T-014. Confirmed by observing the guard demand a major bump for a response field, then accept the same field as optional.
- 2026-09-09 — Claimed by subagent-T-039.
- 2026-09-09 — Status in_progress -> todo: agent lost in the session interruption; no branch and no commits survived.
- 2026-09-09 — Claimed by subagent-T-039.
- 2026-09-09 — Snapshot format 2 records a direction per contract root, derived from the export naming convention and propagated through the live zod graph; the comparison now applies the request or response rule to each. Contract map proven unchanged entry for entry (956 before, 956 after) and the ledger carried across. 69 self-test cases, run inside check on every invocation.
- 2026-09-09 — Status in_progress -> in_review (PR #82).
- Paths widened at review to the two files the work necessarily produced: the
- **The snapshot is shared with T-017, and the board cannot see it.** T-017 owns
- Re-encoding verified rather than asserted: 956 contract entries before and
- 2026-09-09 — Status in_review -> done (PR #82).
