---
id: T-042
title: Enforce the client version floor on the WebSocket handshake
milestone: M5
status: in_review
owner: subagent-T-042
branch: task/T-042-enforce-the-client-version-floor
pr: 100
estimate: S
depends_on: [T-503, T-041]
paths:
  - server/src/websocket/handler.ts
blocked_reason: null
---

# T-042 — Enforce the client version floor on the WebSocket handshake

The minimum-client-version guard runs as a request hook, and the socket upgrade is a raw listener, so no hook runs for it. A client below the floor is refused on HTTP and not on the socket.

This is a missing second line of defence rather than an open door: a listener must register its session over HTTP first, and that call is refused, so it never reaches the handshake. But the two paths disagree about a rule, and the one that disagrees is the one a self-hoster's users hit when something else changes.

The handshake already carries the client identifier in its first frame, so the information is there.

## Acceptance criteria

- [ ] A handshake from a client below the floor is refused, with a close code and reason a client can act on.
- [ ] The refusal names the same floor and the same remedy as the HTTP one. Two different messages for one rule is worse than one message in one place.
- [ ] An absent client identifier is still served, matching the HTTP behaviour: a third-party embedding the client library is not the command line tool and has no version to claim.
- [ ] A malformed identifier is refused rather than treated as absent, so an unparseable claim cannot buy passage.
- [ ] Tests for below-floor, absent, malformed, and current.

## Notes

Raised by T-503, which built the HTTP guard and could not file this itself.

The comparison function lives in the protocol package precisely because both sides need it and neither may import the other across the licence boundary. Use it rather than writing a second one; real version precedence is not string comparison, and getting it wrong locks people out of their own servers.

## Log

- 2026-09-09 — Raised by T-503. The upgrade path runs no request hooks, so the floor it enforces does not reach the socket.
- 2026-09-09 — Claimed by subagent-T-042.
- 2026-09-09 — The onRequest guard genuinely does not cover the upgrade: registerWebSocketEndpoint handles Node's raw upgrade event with ws in noServer mode, so no Fastify hook runs. Enforced it in authenticateUpgrade, on the same X-AgentChat-Client header and before the credential is read, using isClientTooOld and upgradeRequiredMessage from the protocol package so the sentence cannot drift from the HTTP one. The refusal is a 426 and not a close code, because it is decided from the upgrade request and therefore always while HTTP is still available; UpgradeRefusal is CloseReason with the close code made optional, which is the honest shape rather than borrowing 4401. Verified on a running server: below the floor gets a byte-identical UPGRADE_REQUIRED envelope on /ws and on /agents, malformed gets 400, absent and at-the-floor fall through to authentication, and /version stays exempt.
- 2026-09-09 — Half of the task is deliberately NOT done and needs a decision. hello's client field is still only logged, and that is the only place today's CLI announces its version on the socket path: packages/client sends only authorization on the upgrade. Enforcing there is post-handshake, so it needs a new CloseCode in server/src/websocket/frames.ts, outside these paths and forbidden by protocol section 9 -- so I stopped rather than minting 4426. Two ways to finish: (1) send the header on the upgrade, one line in packages/client/src/websocket/listener.ts, no new wire vocabulary at all since protocol 2.2 already says the CLI sends it on every HTTP request and an upgrade is one; or (2) mint the close code, which also needs WsCloseCode and FATAL_CLOSE_CODES in packages/client or a too-old client reconnects forever. Recommend (1).
- 2026-09-09 — Finding for whoever owns server/src/app.ts: refuseUpgrade is called with WWW_AUTHENTICATE_CHALLENGE unconditionally, so the new 426 and 400 both carry a www-authenticate: Bearer header. Nothing in this repository reads it, so it is cosmetic, but it reads as an auth failure to anyone debugging with curl. One conditional, in a file this task does not own.
- 2026-09-09 — Status in_progress -> in_review (PR #100).
