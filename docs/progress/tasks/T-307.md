---
id: T-307
title: Socket registry and router interface
milestone: M3
status: done
owner: subagent-T-307
branch: task/T-307-socket-registry-and-router-interface
pr: null
estimate: M
depends_on: [T-306]
paths:
  - server/src/websocket/registry.ts
  - server/src/routing/router.ts
blocked_reason: null
---

# T-307 — Socket registry and router interface

In-process tracking of which sockets serve which agent and project, behind an interface that a cross-instance implementation can later replace.

## Acceptance criteria

- [ ] Sockets are indexed by the agent and project pair, supporting several sockets per key.
- [ ] A `Router` interface separates "deliver to this agent and project" from how sockets are found, so a Postgres-notify implementation can drop in later.
- [ ] Sockets are removed on close, error, and process shutdown, with no leak under repeated connect and disconnect.
- [ ] Unit tests cover registration, multi-socket fan-out, and cleanup.

## Notes

Plan §4.3, §8. This interface is what unlocks running more than one server instance in v0.2.

## Log

- 2026-09-08 — Claimed by subagent-T-307.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — The churn test found a real bug in its own code: clearing the registry and then releasing a stale handle drove the count negative, because the handle still pointed at a set the map had let go of.
- 2026-09-08 — The cross-instance design is worked out rather than gestured at: notifications carry identifiers only, because Postgres caps a notification at 8000 bytes while a message may be a megabyte.
- 2026-09-08 — Delivery returns a promise even though today's implementation is synchronous, because a signature that grew an await later would be exactly the caller-visible break the interface exists to prevent.
