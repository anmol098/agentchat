---
id: T-035
title: One clock writes updated_at
milestone: M1
status: in_review
owner: subagent-T-035-retry
branch: task/T-035-one-clock-writes-updated-at
pr: 60
estimate: S
depends_on: [T-102]
paths:
  - server/src/db/schema/agents.ts
  - server/src/db/schema/identity.ts
  - server/src/db/schema/messaging.ts
  - server/src/db/schema/tests
  - server/src/services/agents.ts
blocked_reason: null
---

# T-035 — One clock writes updated_at

The `updated_at` columns take their insert value from the database's clock and their update value from the application process's clock. Two different machines write the same column.

```
updatedAt: instant('updated_at').notNull().defaultNow().$onUpdate(() => new Date())
```

`defaultNow` is evaluated by Postgres. The update function is evaluated by Node. Measured against the development container just now, the two disagreed by 65 milliseconds, and the direction of the drift changes.

So `updated_at` can move backwards: a row updated after it was created can carry an earlier timestamp than its own creation, and two updates can land out of order. That is not hypothetical. It surfaced as a failing test asserting a rename bumps the timestamp, where the renamed row's value was four milliseconds behind the created row's.

In production the application and the database are on different hosts by design, so the drift is a deployment property rather than a container quirk. Anything that later reasons about this column, a synchronisation cursor, an audit trail, a cache validator, inherits the problem quietly.

## Acceptance criteria

- [x] One clock writes the column, on both insert and update. Decide which and say why; the database's is the usual answer because it is the one both paths can reach, but the application's is defensible if it is used consistently.
- [x] Every table with this pattern is fixed, not only the one that failed. There are at least two.
- [x] A test proves the value never goes backwards across a create-then-update, and it must fail if the mixture is reintroduced.
- [x] If the fix changes what the database writes, it needs a migration, and that migration must satisfy the compatibility rules.

## Notes

Found while verifying main, from a test that failed once and would have been easy to re-run and forget. The test was right and the schema was wrong.

Check whether `created_at` has the same issue anywhere, and whether any column mixes the two clocks in a less obvious way.

Note the session and message tables were written later and may already be consistent; confirm rather than assuming the problem is limited to the two tables named here.

## Log

- 2026-09-08 — Found on main. Measured 65 ms of drift between the container and host clocks, in a direction that changes.
- 2026-09-08 — Claimed by subagent-T-035.
- 2026-09-08 — Status in_progress -> todo.
- 2026-09-08 — Rate limit killed the agent during setup, before writing code. Nothing to salvage; restarting.
- 2026-09-08 — Claimed by subagent-T-035-retry.
- 2026-09-08 — Reproduced the measurement. The brief's one-shot 'docker exec psql' reads 42-46 ms of skew on this machine, but 110 ms of that round trip is docker exec starting a process; over the pg wire protocol, and over a long-lived psql session, the same container agrees with the host to 0.1-0.4 ms. The defect is real and latent here rather than large here.
- 2026-09-08 — PostgreSQL's clock wins. It is the one both paths already reach: the insert default is now(), services/sessions.ts and services/inbox.ts already write now(), and a replica or a hand-written statement sees it while it cannot see Node's. $onUpdate accepts an SQL fragment, so now() goes in the SET clause and the whole rule stays visible in the schema file - no trigger, no DDL change, no migration, no drift.
- 2026-09-08 — messaging.ts had the same defect at machines.last_seen_at, so it was swept in. identity.ts turned out to have no updated_at column at all - the frontmatter path was optimistic.
- 2026-09-08 — Two less obvious mixtures found and fixed in services/agents.ts: the soft delete wrote agents.deleted_at from new Date() beside a created_at and updated_at that PostgreSQL writes, and it wrote sessions.ended_at from Node while services/sessions.ts writes the same column with now(). One column, two clocks, in two files.
- 2026-09-08 — refresh_tokens is clean: auth/tokens.ts overrides the created_at default and writes created_at, expires_at and revoked_at all from its injected clock, which is the application-clock-used-consistently case the criteria allow. project_invites.expires_at is application-clock while created_at beside it is DEFAULT now(), but each column has one writer and a 30-day deadline compared against the same clock that set it, so it is left alone and noted.
- 2026-09-08 — Status in_progress -> in_review (PR #60).
