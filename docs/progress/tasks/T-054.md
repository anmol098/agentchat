---
id: T-054
title: A socket reset during a token refresh kills the listener outright
milestone: M5
status: blocked
owner: subagent-T-054
branch: task/T-054-a-socket-reset-during-a
pr: null
estimate: M
depends_on: []
paths:
  - packages/client/src/http-transport.ts
  - packages/client/src/tokens.ts
blocked_reason: "The fix is in packages/client/src/websocket/socket.ts, which T-054 does not declare: SocketStream must settle its refusal on 'error' when no 'close' can follow. No in_progress task owns that file; needs the path added."
---

# T-054 — A socket reset during a token refresh kills the listener outright

`agentchat listen` exits with an uncaught exception when a connection is accepted and then reset while it is refreshing its access token:

```text
setTypeOfService EINVAL
```

undici raises it from a socket event rather than from the awaited request, so it lands outside the `catch` in the client's refresh path — the `catch` that exists for precisely this case, "the server is unreachable". An unhandled exception takes the process down, and the whole point of the listener is that it does not go down.

Reproduced on every run by T-509's chaos suite on Node 24.20.0 and macOS, by accepting the connection and then resetting it.

## Acceptance criteria

- [ ] A socket reset at any point in a refresh is handled the way an unreachable server is handled: reported, retried, never fatal.
- [ ] The fix catches the class of problem rather than that one message. An error arriving on a socket event instead of on the awaited promise is the shape to defend against; `EINVAL` from one syscall is a symptom.
- [ ] `SERVER_UNREACHABLE` is the code a `--json` consumer sees, since T-017 added it for exactly this.
- [ ] A regression test that does not depend on the platform. See the note below before writing one.

## Notes

Found by T-509, which **deliberately did not commit a test** for it, and the reasoning is worth keeping: whether that syscall fails on a closed socket is platform-specific, and a test that is red on macOS and green in CI is worse than a written note, because it teaches everyone to ignore a red build. The reproduction is recorded in its module comment and its Log.

So the honest first step is to reproduce it at the level the client actually cares about — an error surfacing on a socket event during an in-flight refresh — rather than to reproduce that syscall.

## Log

- **Confirmed far more strongly, and it now blocks T-509's pull request.** The
- So this is not one platform-specific syscall. `setTypeOfService EINVAL` on
- The fix is what makes T-509 mergeable. Its token test is correct and is
- Reproduce with Node 22:
- `depends_on` corrected at review, and reversed. It read `[T-509]`, which was
- 2026-09-09 — Claimed by subagent-T-054.
- 2026-09-09 — Reproduced on Node 22.23.2 against T-509's chaos suite, then traced it to a single line. The listener does not die during the refresh; it dies before one is ever attempted. On the reconnect after the token expires, the upgrade is refused, and Node 22's global WebSocket fires only 'error' — no 'close' ever follows. SocketStream settles both 'opened' and 'closure' from onClose alone, so WebSocketConnector.connect never returns, the reconnect loop is left awaiting a promise nothing can settle, the event loop empties, and Node exits 13. Minimal proof, no server involved: against a TCP listener that accepts and resets, 'new WebSocket(...)' yields events 'error' on Node 22.23.2 and 'error,close:1006' on Node 24.20.0 — which is exactly why the chaos suite is green on 24 and red on 22. The Node 24 'setTypeOfService EINVAL' is the same class from the other side: net.Socket.prototype.setTypeOfService exists only on Node 24+, which is why one runtime raises it and the other cannot.
- 2026-09-09 — Verified the fix before escalating: making SocketStream settle its refusal when 'error' arrives and the upgrade has not completed turns the chaos suite from 1 failed / 1 passed into 2 passed on Node 22. The change is four lines in packages/client/src/websocket/socket.ts, which this task does not own. Committed on the branch meanwhile is the one piece of the class that is in scope: HttpTransport read the response body outside the catch written for a connection failure, so a socket that died after the headers arrived escaped as a bare TypeError and reached a --json consumer as INTERNAL rather than SERVER_UNREACHABLE. That is fixed with three tests, none of them platform-dependent.
- 2026-09-09 — Status in_progress -> blocked: The fix is in packages/client/src/websocket/socket.ts, which T-054 does not declare: SocketStream must settle its refusal on 'error' when no 'close' can follow. No in_progress task owns that file; needs the path added..
