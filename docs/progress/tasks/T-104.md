---
id: T-104
title: Token service with rotating refresh tokens
milestone: M1
status: done
owner: subagent-T-104
branch: task/T-104-token-service-with-rotating-refresh
pr: null
estimate: M
depends_on: [T-101, T-015]
paths:
  - server/src/auth/tokens.ts
blocked_reason: null
---

# T-104 — Token service with rotating refresh tokens

Issues short-lived access tokens and long-lived refresh tokens, rotating the refresh token on every use so a stolen one has a short useful life.

## Acceptance criteria

- [ ] Access tokens are signed JWTs with a one-hour expiry and the documented claims.
- [ ] Refresh tokens are random 32-byte values stored only as a SHA-256 hash.
- [ ] Refreshing rotates the token and revokes the previous one atomically.
- [ ] Reuse of an already-rotated refresh token revokes the whole chain and returns a distinct error code.
- [ ] Logout revokes the presented refresh token.
- [ ] Unit tests cover expiry, rotation, revocation, and the reuse-detection path.

## Notes

Plan §7. Reuse detection is the difference between a rotating scheme and a decorative one.

## Log

- 2026-09-08 — Claimed by subagent-T-104.
- 2026-09-08 — Authoring agent killed by a rate limit mid-write; tokens.integration.test.ts truncated at line 284. Work preserved on the task branch and handed to a second agent to finish.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — The handover diagnosis was wrong: the test file was not truncated, it had an apostrophe inside a single-quoted string. All eight reported errors cascaded from that one character.
- 2026-09-08 — Fixing it exposed two real type errors the syntax error had masked. The in-memory fake was narrower than the interface it claimed to implement, which is the direction of mismatch that lets a unit suite pass against a fake the real store cannot satisfy.
- 2026-09-08 — 512 green tests were uninformative: vitest transpiles without typechecking, so the compiler had never seen this module. Passing tests are not evidence that a module typechecks.
- 2026-09-08 — Reuse detection revokes the chain in its own transaction that commits before the error is thrown. Inside the rotation transaction it would roll back and leave the attacker's token alive.
