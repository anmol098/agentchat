---
id: T-208
title: Agent commands
milestone: M2
status: done
owner: subagent-T-208
branch: task/T-208-agent-commands
pr: null
estimate: M
depends_on: [T-205, T-109]
paths:
  - packages/cli/src/commands/agent.ts
blocked_reason: null
---

# T-208 — Agent commands

The agent surface: list, create, rename, delete, use, and join.

## Acceptance criteria

- [ ] All subcommands from Plan §6.2 are implemented with `--json` support.
- [ ] `agent use` records the default agent for the current project in user configuration.
- [ ] `agent delete` requires confirmation unless `--yes` is given and explains that history is preserved.
- [ ] Names are validated client-side with the same rule the server enforces, so the error arrives before the round trip.
- [ ] Tests cover each subcommand against a mock server.

## Notes

Plan §6.2, decision D13.

## Log

- 2026-09-08 — Claimed by subagent-T-208.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Deleting an agent forgets every stored default pointing at it. Leaving one is worse than stale, because a default is an identifier: the next command resolves it, fails, and keeps failing even after a replacement is created under the same name.
