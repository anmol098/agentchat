---
id: T-202
title: HTTP client and credential store interface
milestone: M2
status: done
owner: subagent-T-202
branch: task/T-202-http-client-and-credential-store
pr: null
estimate: L
depends_on: [T-201]
paths:
  - packages/client
blocked_reason: null
---

# T-202 — HTTP client and credential store interface

The typed client both the CLI and any future integration use. Handles authentication headers, token refresh, and error translation, and defines the transport seam a daemon can later slot into.

## Acceptance criteria

- [ ] A `Transport` interface abstracts request and connect, with a direct HTTP implementation for v0.1.
- [ ] A 401 triggers exactly one refresh and one retry before surfacing an authentication-required error.
- [ ] A `CredentialStore` interface is defined here with no filesystem access in this package.
- [ ] Server error envelopes become typed client errors that preserve the stable code.
- [ ] Requests carry the client version header.
- [ ] Unit tests against a mock server cover refresh, retry, and error translation.

## Notes

Plan §5, §12.4, decision D1. The transport seam is what keeps the daemon a v0.2 addition rather than a rewrite.

## Log

- 2026-09-08 — Claimed by subagent-T-202.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Concurrent refresh needed two mechanisms: the in-flight promise is assigned synchronously before any await, and a caller arriving after a refresh compares the token that actually failed against the store rather than spending a second refresh. Rotation means a redundant refresh would revoke the chain.
- 2026-09-08 — Found that the frozen set has no code for an unreachable server, so transport failures share INTERNAL with server faults. Filed as T-017.
