---
id: T-509
title: Resilience and failure-mode tests
milestone: M5
status: in_review
owner: subagent-T-509
branch: task/T-509-resilience-and-failure-mode-tests
pr: 92
estimate: L
depends_on: [T-314]
paths:
  - tests/chaos
  - vitest.config.ts
blocked_reason: null
---

# T-509 — Resilience and failure-mode tests

Deliberate abuse of the delivery path, because at-least-once delivery is only worth claiming if it has been tested under failure.

## Acceptance criteria

- [ ] A socket dropped mid-delivery results in redelivery, not a lost message.
- [ ] A server restart with pending inbox rows delivers them once listeners reconnect.
- [ ] Duplicate acknowledgements and out-of-order acknowledgements are harmless.
- [ ] An access token expiring during a long-lived listen is refreshed without dropping a message.
- [ ] A message at the size limit succeeds and one over it is refused cleanly.
- [ ] A slow consumer does not stall delivery to other sessions.

## Notes

Plan §8 M5, §4.4.

## Log

- Declared path corrected before the task started. `server/test/chaos` does not
- 2026-09-09 — Claimed by subagent-T-509.
- 2026-09-09 — Declared paths widened to include vitest.config.ts. tests/chaos matches none of the integration globs, so without it the suite is written and never run — the same gap T-314 hit and did not declare. board check reports no collision: no other in_progress task declares that file.
- 2026-09-09 — Finished the limits and slow-consumer file the previous agent was mid-way through, added the token-expiry file, and opened PR #92. All six acceptance criteria are covered; the suite is 12 tests over the real server, real PostgreSQL and real CLI processes, ~100s, stable over three consecutive runs.
- 2026-09-09 — DEFECT (committed as it.fails): a backlog over 16 MiB can never be replayed. REPLAY_PAGE_SIZE is 100 rows and D10 allows 1 MiB each, so one replay page is up to 100 MiB against MAX_BUFFERED_BYTES of 16 MiB; the socket is closed with 4429 before the handshake reaches ready, nothing is acknowledged, and the next attempt is identical. Seventeen ordinary 1 MiB messages reproduces it. Contradicts protocol section 9.8 and T-032's own acceptance criterion. The fix is a design decision in server/src/routing/delivery.ts and server/src/websocket/handler.ts, which T-509 does not own.
- 2026-09-09 — DEFECT (reported, not committed as a test): agentchat listen dies from an uncaught setTypeOfService EINVAL raised out of undici's socket write path when a connection is accepted and then reset while it is refreshing its token. It is thrown from a socket event, so the catch in the client's #refresh cannot reach it. Reproduced every run on Node 24.20.0 and macOS; left out of the suite because whether that syscall fails on a closed socket is platform-specific and a test red on one platform and green on CI is worse than this note.
- 2026-09-09 — The reconnection tests are written against the behaviour T-041 intends, not the behaviour on main. They pass today only because the heartbeat has no caller, so no socket close marks a session stale. If T-041 wires it as written, without hello reviving a stale session, these tests go red and that is them working. Run this suite against T-041's branch before merging it.
- 2026-09-09 — Status in_progress -> in_review (PR #92).
