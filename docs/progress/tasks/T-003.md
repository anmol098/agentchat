---
id: T-003
title: Vitest configuration and test conventions
milestone: M0
status: todo
owner: null
branch: null
pr: null
estimate: S
depends_on: [T-001]
paths:
  - vitest.config.ts
  - vitest.workspace.ts
  - package.json
blocked_reason: null
---

# T-003 — Vitest configuration and test conventions

Shared test runner setup covering unit tests in every package and a separate integration project that requires a live Postgres.

## Acceptance criteria

- [ ] `pnpm test` runs unit tests for all packages.
- [ ] `pnpm test:integration` runs only tests tagged as integration and fails with a clear message when `DATABASE_URL` is unset.
- [ ] Coverage reporting is enabled and writes to `coverage/`.
- [ ] A sample passing test exists in each project so the wiring is proven.

## Notes

Plan §9. Integration tests use real Postgres, never a mock. Vitest is already installed at the root by T-001, so this task adds configuration and the `test:integration` script only. It owns `package.json` for that script; coordinate before editing anything else in it.

## Log

