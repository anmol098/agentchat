---
id: T-303
title: Message service with idempotent send
milestone: M3
status: done
owner: subagent-T-303
branch: task/T-303-message-service-with-idempotent-send
pr: null
estimate: M
depends_on: [T-301, T-106]
paths:
  - server/src/services/messages.ts
blocked_reason: null
---

# T-303 — Message service with idempotent send

Accepting a message: authorization, size validation, conversation resolution, and idempotency, all committed before any delivery is attempted.

## Acceptance criteria

- [ ] Sender must be owned by the caller and participate in the project; recipient must participate in the same project.
- [ ] Content over 1 MiB is rejected with a distinct error code.
- [ ] Sending without a conversation creates one; `--reply-to` inherits the parent conversation.
- [ ] A repeated `client_message_id` from the same sender returns the original message rather than creating a second one.
- [ ] The message row and its inbox row are committed in one transaction before delivery is attempted.
- [ ] Integration tests cover cross-project rejection, oversize rejection, and duplicate send.

## Notes

Plan §2, §4.3, §4.4. Commit before delivery is what makes at-least-once actually hold.

## Log

- 2026-09-08 — From T-301: the conversation read needs a limit and a cursor. Plan §3 writes it as returning the messages whole, which is exactly the read that cannot avoid a sort.
- 2026-09-08 — Claimed by subagent-T-303.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Caught its own race test being vacuous: the first version passed with the fallback deleted, because the transactions serialised. Replaced with one that pauses the loser mid-transaction, which fails when the fallback is removed.
- 2026-09-08 — Commit before delivery is structural rather than remembered: the module takes no socket registry, no callback and no delivery dependency, so the ordering cannot be forgotten by a later caller.
- 2026-09-08 — Durability is read on a second pool, because a row your own transaction can see proves nothing.
