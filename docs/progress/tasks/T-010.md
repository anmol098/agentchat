---
id: T-010
title: Typecheck root configuration and cross-cutting tests
milestone: M0
status: done
owner: subagent-T-010
branch: task/T-010-typecheck-root-configuration-and-cross
pr: null
estimate: S
depends_on: [T-003]
paths:
  - tsconfig.json
blocked_reason: null
---

# T-010 — Typecheck root configuration and cross-cutting tests

`pnpm typecheck` fans out to workspace members, so nothing at the repository root is checked at all. `vitest.config.ts`, `tests/`, and `scripts/` are invisible to the compiler.

This is not theoretical. T-003 set `coverage.all`, an option Vitest 5 removed. Vite does not validate configuration objects, so the setting was silently ignored at runtime and surfaced only when that task ran the compiler manually against a throwaway configuration. A root project would have caught it, and the next such mistake will not have someone checking by hand.

## Acceptance criteria

- [ ] A root TypeScript project covers `vitest.config.ts` and `tests/`, and `pnpm typecheck` includes it.
- [ ] The root project extends `tsconfig.base.json` and weakens none of its settings.
- [ ] Deliberately introducing a type error in the root configuration fails `pnpm typecheck`.
- [ ] Workspace member type-checking is unaffected and still runs.
- [ ] Reintroducing a removed Vitest option is caught by the compiler.

## Notes

Raised by T-003 during implementation. `scripts/board.mjs` is plain JavaScript with JSDoc rather than TypeScript, so decide deliberately whether to include it via `checkJs` and say which in the pull request.

## Log

- 2026-09-08 — Created from a gap T-003 found: the root of the repository is outside every typecheck project.
- 2026-09-08 — Claimed by subagent-T-010.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Author investigated and correctly concluded the recursive-script problem cannot be solved from tsconfig.json, then stopped rather than editing a file it did not own. The one-line package.json change was applied at review.
- 2026-09-08 — Verified at review: a type error in vitest.config.ts now fails the gate, and the removed coverage.all option is caught. Both passed silently before.
- 2026-09-08 — scripts/board.mjs left out of the project. Including it needs checkJs, which reports 94 errors under this strictness. It wants its own task, not a smuggled one.
