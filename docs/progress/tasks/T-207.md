---
id: T-207
title: Project commands
milestone: M2
status: done
owner: subagent-T-207
branch: task/T-207-project-commands
pr: null
estimate: M
depends_on: [T-205, T-107, T-108]
paths:
  - packages/cli/src/commands/project.ts
blocked_reason: null
---

# T-207 — Project commands

The project surface: list, create, invite, join, leave, init, and current.

## Acceptance criteria

- [ ] All subcommands from Plan §6.2 are implemented with `--json` support.
- [ ] `project join` shows the project name and inviter and asks for confirmation before joining, with `--yes` to skip in non-interactive use.
- [ ] `project init` writes the repository configuration into the working directory.
- [ ] `project current` reports the resolved project and where the resolution came from.
- [ ] Tests cover each subcommand against a mock server.

## Notes

Plan §6.2, PRD §27.

## Log

- 2026-09-08 — Claimed by subagent-T-207.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Made the input descriptor required rather than optional, on the grounds that an optional field with a process fallback is the same workaround wearing an interface. Both prompts now run for real in tests, including the case where nobody answers.
- 2026-09-08 — JSON mode refuses a confirmation rather than skipping it. Proceeding silently would make the formatting flag mean 'and also skip the safety prompt', which is a safety flag hiding inside a formatting one.
