---
id: T-002
title: Biome lint and format configuration
milestone: M0
status: done
owner: subagent-T-002
branch: task/T-002-biome-lint-and-format-configuration
pr: null
estimate: S
depends_on: [T-001]
paths:
  - biome.json
  - .editorconfig
blocked_reason: null
---

# T-002 — Biome lint and format configuration

Single tool for linting and formatting so no task argues about style. Configuration is strict enough to catch real defects, not just cosmetics.

## Acceptance criteria

- [ ] `pnpm lint` and `pnpm format:check` run across every workspace package.
- [ ] Rules ban `any`, unused exports, floating promises, and non-null assertions.
- [ ] `.editorconfig` matches the formatter so editors do not fight it.
- [ ] Running `pnpm format` on the repository produces no diff afterwards.

## Notes

Protocol section 7.1. These commands are the pull-request gate, so they must be fast and deterministic.

## Log

- 2026-09-08 — Claimed by subagent-T-002.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Two-space indent chosen because pnpm rewrites package.json with two spaces on every dependency change; a tab default would leave the tree unformatted after routine work.
- 2026-09-08 — Reformatted scripts/board.mjs (whitespace only), a path outside the declared list. Authorised at assignment; verified the tool still works afterwards.
- 2026-09-08 — Review fix: disabled useLiteralKeys, which contradicts noPropertyAccessFromIndexSignature and would have flagged every process.env read with a fix that breaks the build.
- 2026-09-08 — Follow-up from T-005: useImportExtensions needed forceJsExtensions, and noUndeclaredDependencies is now off for test files only. The false positive on workspace dependencies was correctly predicted here.
