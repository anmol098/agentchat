---
id: T-108
title: Invite routes and service
milestone: M1
status: done
owner: subagent-T-108-retry
branch: task/T-108-invite-routes-and-service
pr: 41
estimate: M
depends_on: [T-106, T-015]
paths:
  - server/src/routes/invites.ts
  - server/src/services/invites.ts
blocked_reason: null
---

# T-108 — Invite routes and service

Invite-based joining: any member may mint a code, anyone may preview it before committing, and joining is idempotent.

## Acceptance criteria

- [ ] Codes use the documented human-readable format and are generated from a cryptographically secure source.
- [ ] Any project member may create an invite; the default expiry is seven days with unlimited uses.
- [ ] Previewing a code returns the project name and inviter without requiring membership, and reveals nothing else.
- [ ] Joining is idempotent, and expired or revoked codes fail with distinct error codes.
- [ ] Integration tests cover preview, join, re-join, expiry, and revocation.

## Notes

Plan section 3, decision D11. Format example: `ANET-7K4M-Q2P9`.

## Log

- 2026-09-08 — Claimed by subagent-T-108.
- 2026-09-08 — Status in_progress -> todo.
- 2026-09-08 — Work lost. The agent stalled without committing, and an orchestrator cleanup removed every agent worktree indiscriminately rather than only the one whose pull request had just merged. Nothing was staged, so no blobs survived.
- 2026-09-08 — Claimed by subagent-T-108-retry.
- 2026-09-08 — Rate limit again, but this time the agent had committed in steps as instructed, so the routes and their unit tests survived on the branch. Only a partial integration test was uncommitted.
- 2026-09-08 — Recovered the rate-limited branch: service, routes and generator tests inherited unchanged, the salvaged integration suite verified green against a real PostgreSQL.
- 2026-09-08 — Added the two missing test files: routes had no unit suite at all, so the pre-PR gate exercised none of the module, and the collision retry plus the seven-day arithmetic were unreachable from a real database.
- 2026-09-08 — Codes are stored in the clear, not digested: the column and its check constraint belong to another task and document the plaintext code, and an invite is a seven-day membership grant rather than a user credential. Flagged in the PR as the change to make if project_invites is ever migrated.
- 2026-09-08 — The acceptance criterion asking for distinct expired-versus-revoked error codes is superseded by the merged protocol package, which makes them one INVITE_INVALID on purpose so the preview cannot confirm that a guessed code was once real. Kept the merged contract.
- 2026-09-08 — Status in_progress -> in_review (PR #41).
- 2026-09-08 — Status in_review -> done.
- 2026-09-08 — Codes are stored in the clear, deliberately. The column is documented as the human-typed code and carries a constraint requiring it to be uppercase, which a digest would satisfy in letter while inverting the column's meaning.
- 2026-09-08 — Preview declares itself public by signature rather than by comment: the method takes no caller, so it cannot branch on membership or widen its answer for one.
- 2026-09-08 — Diverged from a criterion on purpose. It asked for distinct codes for expired and revoked; the merged contract uses one, because distinguishing them confirms a guessed code was once real. The criterion was wrong and is corrected.
