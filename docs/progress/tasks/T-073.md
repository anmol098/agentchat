---
id: T-073
title: Release 0.2.1, so a deployed server stops reporting live listeners as offline
milestone: M5
status: done
owner: claude-fable
branch: task/T-073-release-0-2-1-so
pr: 113
estimate: S
depends_on: [T-069]
paths:
  - package.json
  - packages/cli
  - packages/client
  - packages/protocol
  - server
  - deploy/compose
  - docs
  - README.md
  - CONTRIBUTING.md
blocked_reason: null
---

# T-073 — Release 0.2.1, so a deployed server stops reporting live listeners as offline

The presence fix (T-069) is on `main` and in no release. Every 0.2.0 server marks a listener stale a minute after it connects, so `agentchat agents` reports online agents as offline while messages keep arriving. The maintainer's own deployment shows it. A patch release carries the fix; nothing in it changes the wire protocol or the schema, so the protocol version stays at 5, the minimum client stays at 0.1.0, and the upgrade is a container swap with no migration.

## Acceptance criteria

- [x] Every pinned version reads 0.2.1: the five manifests, `CLI_VERSION`, `SERVER_VERSION`, the example environment file, and the documents that name the current release.
- [x] The release rehearsed from the task branch with a dry-run dispatch before the tag exists, and every job passed.
- [x] `v0.2.1` tagged on `main` after the bump merges; the release workflow publishes the image, the three packages, and the GitHub Release with upgrade notes.
- [x] The upgrade workflow on `main` stays green after the release.

## Notes

The 0.1.0 libraries on npm are unaffected; they are still to be deprecated by the maintainer.

## Log

- 2026-09-12 — Claimed by claude-fable.
- 2026-09-12 — Status in_progress -> in_review (PR #113).
- 2026-09-12 — Status in_review -> done.
- 2026-09-12 — v0.2.1 tagged on the squash of #113 and published by run 34676430998: image tags 0.2.1, 0.2 and latest on GHCR (amd64, arm64), @stackgrid/protocol, @stackgrid/client and @anmol098/agentchat at 0.2.1 on npm with latest moved, and a GitHub Release with upgrade notes saying no migration and a container swap. Rehearsed first from the branch with a dry-run dispatch (run 34676021122), every job green.
