---
id: T-046
title: docs/cli.md describes the streamed message shape wrongly
milestone: M4
status: in_review
owner: subagent-T-046
branch: task/T-046-docs-cli-md-describes-the
pr: 97
estimate: S
depends_on: []
paths:
  - docs/cli.md
blocked_reason: null
---

# T-046 — `docs/cli.md` describes the streamed message shape wrongly

The CLI reference says a `listen --json` `message` event and an `inbox` item match field for field. They do not.

A streamed message carries `recipientAgentId`, and carries **no `recipient` and no `parentMessageId`**. A listed message carries `recipient` and `parentMessageId`. A harness written from the reference — which is the audience this file exists for — breaks on the difference, and breaks at the point where it has already accepted the message.

The two shapes are built in different places and neither knows about the other: the frame in `server/src/routing/delivery.ts`, the listing in `server/src/routes/messages.ts`.

## Acceptance criteria

- [ ] `docs/cli.md` states both shapes accurately, and says plainly that they differ rather than implying they do not.
- [ ] It says which fields are absent from the streamed shape, not only which are present. A harness author needs the absence.
- [ ] Decide and record whether the difference is intended. If the two *should* agree, this task documents the truth and files the follow-up rather than changing the wire; changing a shape is a protocol change and belongs in its own task with a snapshot review.
- [ ] Cross-check every other claim `docs/cli.md` makes about a JSON shape against `docs/protocol.md` and the code while you are in there. One wrong claim of this kind usually has siblings.

## Notes

Found by T-314 while writing the end-to-end suite, which pins both halves with assertions now — so the shapes cannot drift further without a test failing, but the prose is still wrong until this lands.

`docs/protocol.md` is checked against the live schemas by `server/tests/protocol-doc.test.ts`. `docs/cli.md` has no such check, which is why this survived. Consider whether it should, and say what you concluded.

## Log

- 2026-09-09 — Claimed by subagent-T-046.
- 2026-09-09 — Both shapes stated as they are: the streamed message event is the delivery envelope verbatim and carries no recipient at all, omits parentMessageId for a thread root and omits sender when the handle is unresolved; a listed item always carries all three and nulls them instead. The seven other fields are identical. Verified against the built binary, delivery.ts, inbox.ts, protocol.md 9.4 and the MESSAGE_ENVELOPE_KEYS assertion in server/tests/protocol-doc.test.ts, which pins the frame's field set at compile time.
- 2026-09-09 — The difference is intended and nothing on the wire changed. listen passes the envelope through so a newer server's fields need no CLI release; the listing is built by the CLI, which holds the roster and can afford both addresses and stable keys. Making them agree would put a display field on the wire (a protocol change with a snapshot review) or give up the pass-through. No follow-up to change a shape is filed: the defect was the prose.
- 2026-09-09 — Second criterion: npm install --global agentchat replaced with the build-from-clone commands and the alias, all run against this commit, as T-407 recorded that the name is a 404 on the registry. Also added a section for agentchat setup, which the reference omitted while claiming to list commands in --help order and while pointing readers at it from error hints.
- 2026-09-09 — Found outside the declared path, not fixed: packages/cli/src/commands/inbox.ts and conversation.ts repeat the wrong claim in their details text, so the binary prints 'the same message shape agentchat listen --json emits' in --help; examples/shell/agentchat-bridge.sh:246 repeats it in a comment (the script itself is fine, it never reads .recipient); README.md:59 says docs/cli.md 'currently claims they match field for field ... tracked as T-046', which goes stale when this merges.
- 2026-09-09 — On whether docs/cli.md should gain a test, as the notes ask, not built here: yes, and the cheapest honest version is not a parser for the prose. It is (a) assert that every heading of the form '### agentchat <command>' names a command the built binary lists in --help, and every listed command has one — that is the check that would have caught the missing setup section; and (b) extract the fenced JSON examples that are single objects and assert their key sets against the same exported constants server/tests/protocol-doc.test.ts already uses (MESSAGE_ENVELOPE_KEYS for a message event, the CLI's messageJson for an item). Both are field-set comparisons, not prose parsing, and both would have caught this defect. What they cannot catch is a wrong sentence about a shape whose keys are right, which is why this is a judgement about how load-bearing the file is rather than an obvious win; it needs its own task and its own argument.
- 2026-09-09 — Status in_progress -> in_review (PR #97).
