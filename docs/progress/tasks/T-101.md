---
id: T-101
title: Schema and migration for users, projects, memberships, invites and refresh tokens
milestone: M1
status: done
owner: subagent-T-101
branch: task/T-101-schema-and-migration-for-users
pr: null
estimate: M
depends_on: [T-007]
paths:
  - server/src/db/schema/identity.ts
  - server/drizzle
blocked_reason: null
---

# T-101 — Schema and migration for users, projects, memberships, invites and refresh tokens

The identity half of the data model. Everything in M1 depends on these tables existing with the right constraints.

## Acceptance criteria

- [ ] Tables for `users`, `projects`, `project_members`, `project_invites`, and `refresh_tokens` match Plan §2.
- [ ] Unique constraints on `users.github_id`, `users.username`, `projects.slug`, and `project_invites.code`.
- [ ] Foreign keys with deliberate cascade behaviour, documented in a comment where a cascade deletes data.
- [ ] The generated migration applies cleanly to an empty database and is idempotent on re-run.
- [ ] Integration test proves each unique constraint rejects a duplicate.

## Notes

Plan §2. Schema tasks are chained by dependency on purpose so two agents never generate migrations concurrently (Protocol §5).

## Log

- 2026-09-08 — Claimed by subagent-T-101.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Identifiers stored as text holding the whole prefixed string, not a native uuid plus reconstruction. The protocol package forbids a second spelling of an id existing, and reconstruction manufactures exactly that.
- 2026-09-08 — Verified at review against a fresh database: migration applies and re-runs cleanly, wrong-prefix ids are refused, both unique constraints hold, and deleting a user cascades their refresh tokens away.
- 2026-09-08 — The schema physically rejects a token hash that is not a SHA-256, so a plaintext refresh token cannot be stored by accident.
