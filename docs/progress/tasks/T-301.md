---
id: T-301
title: Schema and migration for messaging
milestone: M3
status: todo
owner: null
branch: null
pr: null
estimate: L
depends_on: [T-102]
paths:
  - server/src/db/schema/messaging.ts
  - server/drizzle
blocked_reason: null
---

# T-301 — Schema and migration for messaging

The messaging half of the data model: machines, sessions, conversations, messages, the agent-scoped inbox, and the per-session delivery record.

## Acceptance criteria

- [ ] Tables match Plan §2 including `message_inbox` and the diagnostic `deliveries` table.
- [ ] `client_message_id` is unique per sender so a retried send cannot duplicate a message.
- [ ] Content is limited to 1 MiB at the database level.
- [ ] Indexes support the two hot queries: pending inbox for an agent and project, and a conversation in chronological order.
- [ ] Migration applies cleanly and integration tests cover the constraints and both query plans.

## Notes

Plan §2, §4.3, decisions D3 and D10. The inbox table is agent-scoped, not session-scoped; that is what makes replay correct.

## Log

