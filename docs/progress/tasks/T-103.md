---
id: T-103
title: GitHub OAuth device flow
milestone: M1
status: done
owner: subagent-T-103
branch: task/T-103-github-oauth-device-flow
pr: null
estimate: L
depends_on: [T-101, T-015]
paths:
  - server/src/auth/github.ts
  - server/src/routes/auth.ts
blocked_reason: null
---

# T-103 — GitHub OAuth device flow

The login mechanism. The server brokers the GitHub device flow and upserts a local user, so no password ever exists in this system.

## Acceptance criteria

- [ ] `POST /auth/device/start` returns the user code, verification URI, poll interval, and expiry.
- [ ] `POST /auth/device/poll` returns 428 while pending and tokens on success.
- [ ] Successful authorization upserts the user from the GitHub profile and lowercases the username.
- [ ] GitHub errors including `slow_down`, `expired_token`, and `access_denied` map to distinct stable error codes.
- [ ] The GitHub client secret is never logged or returned.
- [ ] Tests cover the pending, denied, expired, and success paths against a stubbed GitHub.

## Notes

Plan §7, decision D4. Nothing outside this file may assume GitHub is the identity provider.

## Log

- 2026-09-08 — Claimed by subagent-T-103.
- 2026-09-08 — Authoring agent killed by a rate limit before committing. Work preserved on the task branch: typecheck passes and 505 unit tests pass. Handed to a second agent to verify against the criteria and finish.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — The salvage failed two gates the interrupted agent never reached: lint and format. Typecheck and passing tests were not the gate.
- 2026-09-08 — The upsert had zero coverage. The route test substituted a directory, so it proved what the route does with one and nothing about the function that actually upserts and lowercases. Eight integration tests added against real Postgres.
- 2026-09-08 — Extracted the identity seam out of the provider module, so the route no longer imports anything GitHub-named. Verified at review: device codes are stored as digests, and the secret appears in no log, error or message path.
