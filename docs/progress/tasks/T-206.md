---
id: T-206
title: Authentication commands
milestone: M2
status: done
owner: subagent-T-206
branch: task/T-206-authentication-commands
pr: null
estimate: M
depends_on: [T-204, T-103]
paths:
  - packages/cli/src/commands/auth.ts
blocked_reason: null
---

# T-206 — Authentication commands

Login, logout, and whoami. Login drives the device flow with clear instructions and a bounded poll.

## Acceptance criteria

- [ ] `agentchat login` prints the verification URL and user code, then polls at the interval the server specifies.
- [ ] Polling honours the slow-down signal and stops at expiry with a helpful message.
- [ ] `agentchat logout` revokes the refresh token server-side and removes local credentials.
- [ ] `agentchat whoami` reports the user and configured server, and exits 3 when logged out.
- [ ] Tests cover the pending, success, denied, and expiry paths.

## Notes

Plan §6.2, §7.

## Log

- 2026-09-08 — Claimed by subagent-T-206.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Handled all four poll outcomes by reading the server rather than the client's comment, which documents only two. The mapping is keyed by wire code and valued by decision, so adopting a dedicated rate-limit code later moves one key.
- 2026-09-08 — A failed logout keeps the credentials and exits non-zero. Deleting them would leave a live token on the server that can now never be revoked, under a message saying you are signed out.
- 2026-09-08 — Found that no default server URL exists anywhere, so every networked command fails on a fresh install. Filed as T-026.
