---
id: T-209
title: Status command
milestone: M2
status: done
owner: subagent-T-209
branch: task/T-209-status-command
pr: null
estimate: S
depends_on: [T-205]
paths:
  - packages/cli/src/commands/status.ts
blocked_reason: null
---

# T-209 — Status command

One command that answers "why is this not working": resolved context, credential state, server reachability, and the caller own sessions.

## Acceptance criteria

- [ ] Reports resolved project and agent with the source of each resolution.
- [ ] Reports login state, server URL, and server reachability with the server version.
- [ ] Lists the sessions belonging to the resolved agent.
- [ ] Works when logged out and when no project is configured, explaining what to do next rather than failing.
- [ ] `--json` output is stable and documented.

## Notes

Plan section 6.2. This is the first command a confused user or agent will run, so its output is a diagnostic, not a status line.

## Log

- 2026-09-08 — Claimed by subagent-T-209.
- 2026-09-08 — Status in_progress -> done.
- 2026-09-08 — Exits zero whenever a report was produced. The command asked for is the report, and a non-zero exit would abort a preflight script under set -e before anyone reads the explanation it just printed. The machine signal is in the payload instead.
- 2026-09-08 — Never refuses to run. Broken user config, a credential-shaped key in the repository config, corrupt credentials and an unreachable server are all reported lines rather than throws.
- 2026-09-08 — From T-024: the credentialsFileFor workaround in the status command can now be deleted; the shared resolver produces the identical path in every environment tested.
